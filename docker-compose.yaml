services:
  postgres:
    image: postgres:17.9-alpine
    container_name: trust-forge-postgres
    ports:
      - ${DATABASE_PORT}:5432
    volumes:
      - trust-forge-db:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: ${DATABASE_USERNAME}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: ${DATABASE_NAME}

  maildev:
    container_name: trust-forge-maildev
    build:
      context: .
      dockerfile: maildev.Dockerfile
    ports:
      - ${MAIL_CLIENT_PORT}:1080
      - ${MAIL_PORT}:1025

  redis:
    image: redis:7-alpine
    ports:
      - 6379:6379

volumes:
  trust-forge-db:
